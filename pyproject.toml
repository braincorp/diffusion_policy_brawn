[project]
name = "diffusion_policy_brawn"
description = "Diffusion Policy for integration with Brawn."
requires-python = ">=3.12"
dynamic = ["version"]
dependencies = [
    "diffusers==0.30.3",
    "hydra-core==1.3.2",
    "pandas==2.2.3",
    "zarr==2.18.3",
]
